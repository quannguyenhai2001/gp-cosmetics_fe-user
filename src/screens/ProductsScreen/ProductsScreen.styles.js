import { makeStyles } from '@mui/styles';

export default makeStyles((theme) => ({
    screen: {
        minHeight: '100vh',
        paddingTop: '3rem',
        paddingBottom: '3rem',
    },
    arrow: {
        fontSize: "24px",
        fontWeight: "bold",
        transform: "rotate(45deg)",
        color: "red",
    }





}));